services:
  db-mock:
    container_name: mock-db
    build:
      context: .
      dockerfile: ./Dockerfile-db
    environment:
      - POSTGRES_USER=mock
      - POSTGRES_PASSWORD=mock
      - POSTGRES_DB=mock
    ports:
      - 5432:5432